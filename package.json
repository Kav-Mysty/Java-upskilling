{
  "name": "bootstrap-sass-project",
  "version": "1.0.0",
  "scripts": {
    "build-css": "sass style.scss style.css --no-source-map --style=compressed",
    "watch-css": "sass --watch style.scss:style.css"
  },
  "devDependencies": {
    "bootstrap": "^5.3.0",
    "sass": "^1.64.0"
  }
}
